<view class="index">
  <image class="top-bg" mode="widthFix" src="{{baseURL}}/top-bg.png"></image>
  <image class="telecom" mode="widthFix" src="{{baseURL}}/telecom.png"></image>
  <view class="options">
    <text data-type="complain" bindtap="showDialog">投诉</text> |
    <text data-type="rule" bindtap="showDialog">活动规则</text>
  </view>
  <view class="draw">
    <image class="box-bg" mode="widthFix" src="{{baseURL}}/box-bg.png"></image>
    <swiper autoplay interval="{{3000}}" circular vertical wx:if="{{recodeList.length > 0}}" class="drwan-pirze">
      <swiper-item wx:for="{{recodeList}}" wx:key="{{index}}">
        恭喜{{item.phn}}获得{{item.prizeName}}
      </swiper-item>
    </swiper>
    <text class="tip" bindtap="scrollToTip">看看盒子里都有啥</text>
    <view class="prize">
      <view wx:for="{{prizeList}}" wx:key="item.id">
        <image bindtap="getPrize" data-id="{{item.id}}" mode="widthFix" src="{{item.image}}"></image>
        <text class="{{item.image === (baseURL + '/prize-bg02.png') ? 'active' : ''}}">{{item.text}}</text>
      </view>
    </view>
    <view class="btn-bg">
      <image mode="widthFix" src="{{baseURL}}/btn-bg.png"></image>
      <text class="open" bindtap="openPrize">开启礼盒</text>
    </view>
  </view>
  <view class="prize-type">
    <view class="tip" id="tip">心愿盒子大揭秘</view>
    <view class="type">
      <text data-menu="{{1}}" class="{{menu === 1 ? 'active' : ''}}" bindtap="changeItem">重磅好礼包邮送</text>
      <text data-menu="{{2}}" class="{{menu === 2 ? 'active' : ''}}" bindtap="changeItem">超值业务五折办</text>
      <text data-menu="{{3}}" class="{{menu === 3 ? 'active' : ''}}" bindtap="changeItem">特惠福利享不停</text>
    </view>
    <view class="items" wx:if="{{menu === 1}}">
      <image src="{{baseURL}}/left-prize01.png" mode="widthFix"></image>
      <image src="{{baseURL}}/left-prize02.png" mode="widthFix"></image>
      <image src="{{baseURL}}/left-prize03.png" mode="widthFix"></image>
      <image src="{{baseURL}}/left-prize04.png" mode="widthFix"></image>
    </view>
    <view class="items" wx:if="{{menu === 2}}">
      <image src="{{baseURL}}/middle-prize.png" mode="widthFix"></image>
    </view>
    <view class="items" wx:if="{{menu === 3}}">
      <image src="{{baseURL}}/right-prize.png" mode="widthFix"></image>
    </view>
  </view>
  <swiper indicator-dots autoplay interval="{{3000}}" circular>
    <swiper-item>
      <image mode="widthFix" src="{{baseURL}}/ad1.jpg"></image>
    </swiper-item>
    <swiper-item>
      <image mode="widthFix" src="{{baseURL}}/ad2.jpg"></image>
    </swiper-item>
    <swiper-item>
      <image mode="widthFix" src="{{baseURL}}/ad3.jpg"></image>
    </swiper-item>
    <swiper-item>
      <image mode="widthFix" src="{{baseURL}}/ad4.jpg"></image>
    </swiper-item>
    <swiper-item>
      <image mode="widthFix" src="{{baseURL}}/ad5.jpg"></image>
    </swiper-item>
  </swiper>
</view>

<!-- 活动规则 dialog -->
<view data-layer wx:if="{{ruleDialog}}" class="layer" bindtap="close">
  <view class="dialog">
    <view class="header">
      <view>活动规则</view>
    </view>
    <scroll-view scroll-y class="body rule">
      <view>1. 活动时间：2018年12月7日-12月15日</view>
      <view>2. 活动对象：湖南电信用户</view>
      <view>3. 用户初次参与活动有1次抽现金红包机会，每成功邀请1位好友首次关注“中国电信湖南客服”即可获得1次抽取现金红包机会。</view>
      <view>4. 现金红包金额可累加，达到1元即可提现到微信账户中，如未关注绑定“中国电信湖南客服”红包将无法提现。</view>
      <view>5. 因腾讯原因导致红包被拦截，或因用户24小时未领取红包导致逾期退回等情况，中国电信湖南客服不予补发。</view>
      <view>6. 每天红包提现总金额为10000元，提现时间为每天12：00至18：00,本活动旨在回馈新老用户，严禁一切恶意刷奖行为，一经查实，活动账户将被冻结清零。</view>
      <view>7. 本活动最终解释权归中国电信湖南客服所有。</view>
    </scroll-view>
    <view class="footer">

    </view>
  </view>
</view>

<!-- 投诉 dialog -->
<view data-layer wx:if="{{complainDialog}}" class="layer" bindtap="close">
  <view class="dialog">
    <view class="header">

    </view>
    <scroll-view scroll-y class="body complain">
      <view class="reason">请选择投诉原因</view>
      <view data-id="{{item.id}}" class="list {{item.select ? 'active' : ''}}" bindtap="selectReason" wx:for="{{complainList}}" wx:key="{{index}}">
        <text>{{item.reason}}</text>
        <text wx:if="{{item.select}}">✔</text>
      </view>
    </scroll-view>
    <view class="footer">
      <button>提交</button>
    </view>
  </view>
</view>