<view data-layer class="layer" bindtap="close">
  <view class="dialog">
    <view class="header">

    </view>
    <scroll-view scroll-y class="body complain">
      <view class="reason">请选择投诉原因</view>
      <view data-id="{{item.id}}" class="list {{item.select ? 'active' : ''}}" bindtap="selectReason" wx:for="{{complainList}}" wx:key="{{index}}">
        <text>{{item.reason}}</text>
        <text wx:if="{{item.select}}">✔</text>
      </view>
    </scroll-view>
    <view class="footer">
      <button>提交</button>
    </view>
  </view>
</view>